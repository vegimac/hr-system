> Verbatim aus CLAUDE.md ausgelagert am 29.08.2026 (Kosten-Verschlankung).
> Inhalt gilt UNVERÄNDERT weiter — alle ABSOLUT-Regeln bleiben ABSOLUT.
> Nichts wurde gekürzt oder umformuliert; nur der Speicherort ist neu.

### Swissdec ELM 6.0 (Konzept 27.08.2026)

Elektronische Lohnmeldung: Konzept + Etappenplan E1–E9 in `docs/swissdec-elm6-konzept.md`; komplette ELM-6.0-Doku (Richtlinien 507 S., Transmitter-Richtlinien, WSDL/XSD/Beispiel-XMLs, QST-Excel-Beispiele) unter `docs/swissdec/`. **Zertifizierungsbasis = ELM 6.0** (nicht 5.x). Meldeeinheit = Rechtseinheit/UID (Schaub Restaurants GmbH, Filialen = Workplaces). Walter hat Zugang zur Swissdec-Testinfrastruktur (Benutzer «schaub»: Infopoint, Quality Tool, Refapps Receiver/Transmitter). Unser ELM-Lohnraster (17.08.) + Katalog-Flag-Basen sind die Vorarbeit. Ziel: erste scharfe Jahresmeldung fürs Lohnjahr 2027 (Übermittlung Anfang 2028); QST läuft bis zur Zertifizierung über die bisherigen kantonalen Kanäle. **E1-Stand 27.08.2026:** Admin-Bereich «Swissdec» (Sidebar, nur admin; `page-swissdec`, `js/swissdec.js`, `Services/Elm/ElmTransmitterClient.cs`, `ElmController`) gebaut; **Ping grün gegen Prod-Distributor UND Refapps Receiver**. CheckInteroperability wird auf BEIDEN Umgebungen mit Fault `Client.security` abgewiesen — ab dieser Operation verlangt Swissdec WS-Security (signieren MIT Transmitter-Zertifikat + verschlüsseln; Reihenfolge Signatur→Encryption, nur Ping ist ausgenommen). **Das Transmitter-Zertifikat stellt Swissdec (eigene CA) erst nach/über den Zertifizierungsprozess aus — es gibt KEINEN öffentlichen Test-Keystore** (Quelle: `docs/swissdec/SecurityTransmitter_d.pdf` Kap. 3; `RefApps_Schnelleinstieg.pdf` liegt ebenfalls dort). Übungsweg ohne Zertifikat = **Refapps-Transmitter-Webapp** (XML hochladen, Refapps signiert selbst) → E2–E4 laufen darüber. Ping laut Richtlinien NUR manuell auslösen, nie automatisiert. Die «Certificates»/RegisterOrganizationAuthentication-Seiten im Refapps-Transmitter betreffen das SUA-Zertifikat (Unternehmens-Ausweis), NICHT das Transmitter-Zertifikat. **E2-Stand 28.08.2026:** `ElmAnnualDeclarationBuilder` (DeclareAnnualSalary, Domäne AHV, aus PayrollSnapshots ALLER Filialen, mit `<sdc:TestCase/>`-Flag) + `ElmXmlValidator` (XSD-Set aus `Assets/Swissdec/` — NUR die 5 Service-Schemas laden, die noNS-Tax-XSDs dort kollidieren) + `GET /api/elm/annual-ahv/{year}` + E2-Karte auf page-swissdec (XML erzeugen/prüfen/herunterladen). E2-Vereinfachungen (E3/E5 ersetzen): AK-Nr./UID Platzhalter wenn CompanyProfile leer, ALV = monatlich auf 12'350 gedeckelte Summe (Näherung), UVG/BVG-Institution als NoneWithReason. Übungs-XML NUR aus test.onecrew.ch-Kunstdaten erzeugen und im Refapps-Transmitter hochladen — nie Echtdaten. Struktur gegen die offiziellen Schemas mit lxml verifiziert (Pflicht in ParticularsAddressType: `ResidenceCanton`; Steady braucht WeeklyHours+ActivityRate; SV-Nr.-Pattern 756.xxxx.xxxx.xx sonst `<c:unknown/>`). **E2 ERFOLGREICH ÜBERMITTELT 28.08.2026:** Kunstdaten-Jahresmeldung (5 Kunst-MA `999001–999005` auf test.onecrew.ch, Lohnlauf Juli 2026) via Refapps-Transmitter → DeclareAnnualSalary SUCCESS, GetStatus ACCEPTED (Jackpot Institution = Mock-Empfänger bei unbekannter AK-Nr.), Completion-Link freigegeben — voller Zyklus inkl. Browser-Completion (`COMPLETION_RELEASE_MISSING` = Freigabe-Dialog nötig). Lehren Testinstanz: `TRUNCATE employee CASCADE` löscht auch `app_user` (FK!) → Service-Neustart legt Erst-Admin neu an; Kunstdaten-SQL in `migrations-archive/` nicht nötig (einmalig, im Chat); Download-Buttons brauchen Fallback ohne `new Blob()` (Browser-Erweiterung «location-spoofing» kapert den Konstruktor → try/catch mit data:-URL-Anker, umgesetzt in swissdec.js elmAnnualDownload). **E3-Stand 28.08.2026:** Tabelle `elm_stammdaten` (EINE Zeile pro Rechtseinheit: UID, AK-Name/Kassen-Nr./Abrechnungs-Nr., FAK, UVG/UVGZ/KTG/BVG Versicherer+Kunden-+Vertrags-Nr.; Migration idempotent in Program.cs + `migrations-archive/add_elm_stammdaten.sql`), Endpoints `GET/PUT /api/elm/stammdaten` (ElmController, admin), Erfassungskarte E3 auf page-swissdec. `ElmAnnualDeclarationBuilder` liest zuerst elm_stammdaten (Kassen-Nr. = Addressee, Abrechnungs-Nr. = AK-CC-CustomerNumber), Fallback CompanyProfile → Platzhalter+Hinweis. Zertifizierung: NUR EIN Verfahren («volles Programm», keine vereinfachte Variante — Walter-Info 28.08.2026); Perspektive Lizenzierung an ~100 Franchisenehmer-Restaurants in Erstberatungs-Doku (`docs/Swissdec-Erstberatung-OneCrew.docx`) aufgenommen. **E3 ERLEDIGT 28.08.2026 (Ende-zu-Ende verifiziert):** Führende Quelle = EMPFÄNGER-KATALOG (`LohndatenEmpfaenger` zentral + `CompanyProfileEmpfaenger` Mitglied-/Subnummer pro Filiale; neue Art «UVGZ»; UID-Feld im Empfänger-Modal — Lade-Bug in der Filial-Projektion behoben); E3-Karte erfasst NUR noch UID Rechtseinheit + UVG/BVG «versichert seit», zeigt Katalog-Werte read-only (`GET /api/elm/stammdaten/vorschlag`); Builder liest Katalog direkt (AK-Kassen-Nr. = Addressee, gemeinsame Mitgliednummer = CustomerNumber sonst Hinweis; UVG/BVG-Insurance mit Name+UID+ValidAsOf statt NoneWithReason; BUR-Nummer im Workplace bei Pattern `[A-Z][0-9]{8}`). Kunstdaten-Meldung mit Katalog-Nummern (046.000/629.0714.00) via Refapps übermittelt → Business State FINISHED. Fachlich geklärt: jede Filiale = Zweigniederlassung mit EIGENER UID + EIGENEN Mitgliednummern bei AK/Versicherern; Meldungskopf = Hauptsitz-UID (Schaub: Meggen); Multi-Rechtseinheit (Lizenznehmer mit 2 GmbHs) = Design-Vorgabe für E5 im Konzept. **Hauptsitz-Verwaltung (Walter-Vorgabe 29.08.2026):** Tabelle `hauptsitz` (Name, UID, Sitzadresse; mehrere möglich) + `company_profile.hauptsitz_id` (FK, ON DELETE SET NULL; Migration idempotent + `migrations-archive/add_hauptsitz.sql`). UI: System → Filialen & Benutzer → Kachel «Hauptsitze» (`page-hauptsitze`, `js/hauptsitze.js`, Icon `img/hauptsitz-karte.png` — von Walter geliefertes Skizzen-Bild 29.08.2026; die alte `hauptsitz-karte.svg` liegt ungenutzt daneben), Zuordnung (Walter 29.08.2026, «war zu versteckt»): **Inline-Dropdown GANZ OBEN im Stammdaten-Tab, noch VOR dem Titel** (`stmHauptsitzInline-{id}`, Sofort-Speichern via eigenem Mini-Endpoint `PATCH /api/companyprofiles/{id}/hauptsitz` — NICHT via PATCH /stammdaten, das ist ein Voll-Ersatz und würde alle anderen Felder nullen!); zusätzlich im Bearbeiten-Modal ganz oben (`stmHauptsitz`, Teil des Stammdaten-Voll-Saves). `HauptsitzController` (api/hauptsitze, admin; EditLock-whitelisted). ElmAnnualDeclarationBuilder: UID+Firmenname der Meldung aus dem zugeordneten Hauptsitz (mehrere Hauptsitze → Hinweis, Meldung pro Rechtseinheit erst E5); E3-Karte zeigt UID nur noch read-only aus der Hauptsitz-Verwaltung.

